import { TestBed } from '@angular/core/testing';

import { SseService } from './sse.service';

describe('SseService', () => {
  let service: SseService<any>;

  beforeEach(() => {
    TestBed.configureTestingModule({});
  });

});
